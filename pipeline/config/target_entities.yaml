# Юрисдикции (iso2), чьи документы по умолчанию классифицируются в трек target-entity
# (discovery/manual.py::_default_track) — не сравнительный опыт других государств
# (intl-xperience), а ПЕРВИЧНЫЙ объект анализа проекта, для которого пишется итоговый
# пакет предложений. Правится ВРУЧНУЮ; трекается в git; НЕ зашивать список в код —
# расширение фокуса на новую юрисдикцию = новая строка здесь, без правки кода.

jurisdictions:
  - me   # Черногория — единственный target проекта на сегодня (2026-07-25)
