# Тюнинг-параметры триажа релевантности источников (source-relevance-triage).
# Правится ВРУЧНУЮ; НЕ зашивать значения в код. Трекается в git (см. .gitignore —
# /pipeline/config/ не игнорируется). Потребитель — будущий автоматический судья
# Стадии 1 (отложен, вне скоупа спека); пока файл — human-facing источник значения
# для ручного триажа.

# Год-ориентир «планки фронтира» агентного G2AI (спек §2.1): документы по УЗКОЙ оси
# (agentic_g2ai) старше этого года, как правило, до-агентные. Планка — сигнал, не
# жёсткий гейт: несущий до-фронтирный фундамент (X-Road-шины, интероперабельность)
# по широкой оси остаётся релевантным. MCP анонсирован Anthropic в ноябре 2024 →
# агентное правительство ≈ 2025+. Пересматривать по мере сдвига фронтира.
frontier_year: 2025
