# Runtime-зависимости пайплайна G2AI-корпуса.
# Установка: uv pip install -r requirements.txt
pdfplumber==0.11.10   # PDF -> Markdown (pdf_to_markdown.py)
trafilatura==2.1.0    # HTML -> Markdown (convert/converters.py: _convert_html)
mammoth==1.12.0       # DOCX -> HTML (convert/converters.py: _convert_docx, custom image-handler)
markdownify==1.2.3    # HTML -> Markdown (там же, сабкласс _DocxMarkdownify)
lxml==6.1.1           # ELI-разметка -> <hN> до trafilatura (convert/eli.py); транзитивно у trafilatura, здесь — прямой импорт
openpyxl==3.1.5       # XLSX -> Markdown (convert/converters.py: _convert_xlsx, convert/xlsx_charts.py)
mermaidx==0.9.3       # render-валидация mermaid-вывода настоящим mermaid.js (embedded QuickJS, без Node/браузера) —
                      # chart_render.py::_mermaid_renders, штатный gate перед выдачей mermaid-блока (не dev-only)
pydantic==2.13.4      # схема метаданных sources.yaml (schema.py)
PyYAML==6.0.3         # чтение/запись sources.yaml и словарей vocab/
networkx==3.6.1       # гетерогенный граф знаний (build_graph.py)
tokenizers==0.23.1    # токенизатор bge-m3 для канонической чанковки (bge_tokenizer.py)
numpy==2.5.1          # векторные операции (embed.py, vector_store.py)
onnxruntime==1.27.0   # инференс bge-m3 int8 ONNX, CPU (embed.py)
