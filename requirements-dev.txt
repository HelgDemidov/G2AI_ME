# Dev/CI-инструменты (проверки pipeline/scripts/).
# Установка:  uv pip install -r requirements-dev.txt
# Проверки (ИЗ КОРНЯ репозитория; конфиг — pyproject.toml), те же, что в CI (.github/workflows/ci.yml):
#   .venv/bin/ruff check pipeline/scripts
#   .venv/bin/python -m mypy pipeline/scripts
#   .venv/bin/python -m pytest -m "not model and not corpus" --cov --cov-report=term-missing
#   .venv/bin/python pipeline/scripts/validate_sources.py
-r requirements.txt
mypy==2.3.0
ruff==0.15.21
pytest==9.1.1
pytest-cov==7.1.0
types-PyYAML==6.0.12.20260518
types-openpyxl==3.1.5.20260518
reportlab==5.0.0       # синтетические PDF-фикстуры для тестов pdf_to_markdown (генерация на лету, ничего не коммитится)
types-reportlab==4.5.1.20260712
hypothesis==6.157.2    # property-based тестирование геометрии pdf_to_markdown (test-coverage-hardening); общепроектный стандарт
# mermaidx — теперь runtime-зависимость (requirements.txt), тянется сюда транзитивно через -r requirements.txt выше
